\subsubsection{Longhorn}
Longhorn ist eine Storage-Class. Storage Classes erstellen dynamisch Persistent Volumes für Persistent Volume Claims.
Longhorn erstellt dabei nicht nur ein Persistent Volume, sondern Replicas über mehrere Nodes.
Das ist für eine HA-Architektur wichtig, damit beim Storage kein Single Point of Failure entsteht.